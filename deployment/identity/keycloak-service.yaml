apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: zt-identity
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: http-management
      port: 9000
      targetPort: 9000
  selector:
    app: keycloak
