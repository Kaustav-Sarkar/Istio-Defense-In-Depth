apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: holiday_calendar
  rules:
    - actions: ["view", "list"]
      effect: EFFECT_ALLOW
      roles: ["employee", "manager", "hr_admin", "it_admin", "public_data_admin", "security_auditor"]
    - actions: ["create", "update", "delete"]
      effect: EFFECT_ALLOW
      roles: ["hr_admin", "public_data_admin"]
